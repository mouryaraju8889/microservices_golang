apiVersion: v1
kind: Service
metadata:
  name: orderservice
spec:
  selector:
    app: orderservice  # Matches the deployment's label selector
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP  # This will expose the service externally (for Minikube)
